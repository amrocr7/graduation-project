name: salah_code
description: الكود - تطبيق المحافظة على الصلاة
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^17.0.0
  shared_preferences: ^2.2.3
  timezone: ^0.9.4
  workmanager: ^0.7.0
  audioplayers: ^6.0.0
  geolocator: ^12.0.0
  permission_handler: ^11.3.0
  share_plus: ^9.0.0
  file_picker: ^8.0.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/
